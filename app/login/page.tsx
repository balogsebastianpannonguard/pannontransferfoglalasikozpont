import LoginClient from "./LoginClient";

export const metadata = {
  title: "Belépés | Pannon Transfer Admin",
  description: "Jelentkezz be az admin felületre.",
};

export default function LoginPage() {
  return <LoginClient />;
}
